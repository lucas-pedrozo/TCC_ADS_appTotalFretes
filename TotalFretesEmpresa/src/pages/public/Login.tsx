import bgLogin from '../../assets//login.png';

function Login() {
    return (
        <main id="login" className="flex items-center justify-center h-screen" style={{ backgroundImage: `url(${bgLogin})`, backgroundSize: 'auto', backgroundPosition: 'center' }} >
            
        </main>
    );
}

export default Login;